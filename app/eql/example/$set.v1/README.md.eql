<%
    if Item == "string" {
        NewArgs, AddArgs, HasArgs = `"a", "b"`, `"c"`, `"d"`
    } else {
        NewArgs, AddArgs, HasArgs = `1, 2`, `3`, `4`
    }
%>
$set.v1
======

$Set is a set of $Item, implemented via `map[$Item]struct{}` for minimal memory consumption. Here is an example:

```go
import (
    "$set.v1"
)

set := $set.New($NewArgs)
set.Add($AddArgs)

if set.Has($HasArgs) {
    println("set has item `$HasArgs`")
}
```
